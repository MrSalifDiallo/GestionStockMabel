{
  "build": {
    "commands": [
      "cp -r backend/. .",
      "composer install --no-dev --no-interaction",
      "npm ci --production",
      "npm run build",
      "php artisan config:cache"
    ]
  }
}
